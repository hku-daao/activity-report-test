### 2026-04-27 ###

after login, put the next before the logout button, and remove the session "your information". The whole dashboard will be moved to another page. This page will only be shown after clicking a newly created button "View Acitivity Reports". The first page should be renamed to "Activity Report: Home". "Create Activity Report" button should be renamed to "New: Meeting / Engagement / Activity Report"

Name of the login user, orginally at the "your information" session, should be put before logout button.

There should be a back button other than the home page. Other links, such as "View Activity Reports", "New: Meeting..." at the top bar should be removed.

At the "New: Meeting / Engagement / Activity Report" page, Add a checkbox of "multiple days event". If it's checked, then hide the duration part. And then add "End Date/Time" for user to select with. Rename "The other party's name:" to "Donor / Prospect / Guest:" and make it a multiple line box. Remove the "Submit" button, and there should no longer be status such as "submitted / draft / unsubmitted". User can always edit.

For the "Donor / Prospect / Guest" box, user can input names in multiple lines, one name per line. Then add a split name button. After clicking the buttons, then split the names input multiple boxes.

Do the same with "Other People" field. And rename "Other People" to "Other colleagues"

When I click on "Split names", it should add on top of the current list, instead of replacing them.

At home page, add a button call "Daily Journal" before "New: Meeting...". The button "View Activity Reports" should move to the bottom. For the "Daily Journal" function, it should call a page and create an entry with title "Journal of yyyy-mm-dd", where yyyy-mm-dd is today's day. Then it will provide a multiple line freetext box for user to enter. The system will allow user to "save". When people click "Daily Journal" but today's Journal has already created, then the system will automatically call the existing today's journal for further editing instead of creating another entry. The system should also record who's the creator. Then at home page, add a button "View Journal" before "View Activity Reports", and it will call a dashboard page to show all the created journal.

Add another button call "Proactive initiative and Activity", this function is similar to "Daily Journal", but the title is entered by the user instead of freely generated. A separate dashboard should be created. At home page, all dashboard buttons should be group into "Dashboard", the other buttons group into "Create New".

Create New session of home page should be at the top.

"View Activity Reports" should be renamed to "View Meeting / Engagement / Activity Reports". "New: Meeting / Engagement / Activity Reports" should be renamed to "Meeting / Engagement / Activity Reports". "Proactive initiative and Activity" should be renamed to "Proactive Initiative and Activity". "View proactive initiative and activities" should be renamed to "View Proactive Initiative and Activity".

All buttons in "Create New" session should use same color tone of "Daily Journal".



### 2026-04-28 ###

For the edit page insert "attachment link" function. Other than user enters attachment links, please add an option allowing user to upload their own files in PC or mobile phones. It should connect to Firebase storage, folder configurable in .env file. The download links should not be viewable to user for security reason. 

what should be configured at VITE_FIREBASE_STORAGE_ATTACHMENTS_PREFIX

how to fix "Firebase Storage: User does not have permission to access 'activity_report/giWdsfmikvQJqxHGT5hjuaiyEtW2/proactive/381c201b-b6a2-4f39-b798-2880ca956685/1777343161495_test.txt'. (storage/unauthorized)"

after i upload a file and click download, there is no file downloaded. And when I go back to home page and back to specific entries, the upload file list disapear at all. 

after i click upload file, then it should auto save into DB. and download button is still not working.

The download function is neither working in windows or mobile browser.

Download did open at another tab this time, but still not triggering any download file action.

No, i still don't see download action triggered

error in web-brower: Access to XMLHttpRequest at 'https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…201b-b6a2-4f39-b798-2880ca956685%2F1777343749127_test_excel.xlsx?alt=media' from origin 'http://localhost:5173' has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
proactiveAttachmentStorage.ts:66 
 GET https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…201b-b6a2-4f39-b798-2880ca956685%2F1777343749127_test_excel.xlsx?alt=media net::ERR_FAILED 304 (Not Modified)
await in makeRequestWithTokens		
getBlobData	@	proactiveAttachmentStorage.ts:66
(anonymous)	@	proactiveAttachmentStorage.ts:166
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:636
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:657

still error: 381c201b-b6a2-4f39-b798-2880ca956685:1 Access to fetch at 'https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…edia&token=c8853b60-8a27-4478-8cd8-ebe5b9eb2020&_dlcb=1777345030710=717659' from origin 'http://localhost:5173' has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
proactiveAttachmentStorage.ts:70 
 GET https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…edia&token=c8853b60-8a27-4478-8cd8-ebe5b9eb2020&_dlcb=1777345030710=717659 net::ERR_FAILED 200 (OK)
getBlobData	@	proactiveAttachmentStorage.ts:70
await in getBlobData		
(anonymous)	@	proactiveAttachmentStorage.ts:162
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:636
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:657
<button>		
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:622
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:335
<ProactiveInitiativeEditorPage>		
App	@	App.tsx:115
<App>		
(anonymous)	@	main.tsx:8

Error: 381c201b-b6a2-4f39-b798-2880ca956685:1 Access to XMLHttpRequest at 'https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…201b-b6a2-4f39-b798-2880ca956685%2F1777343749127_test_excel.xlsx?alt=media' from origin 'http://localhost:5173' has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
proactiveAttachmentStorage.ts:90 
 GET https://firebasestorage.googleapis.com/v0/b/daao-a20c6.firebasestorage.app/…201b-b6a2-4f39-b798-2880ca956685%2F1777343749127_test_excel.xlsx?alt=media net::ERR_FAILED 304 (Not Modified)
await in makeRequestWithTokens		
tryGetBytes	@	proactiveAttachmentStorage.ts:90
(anonymous)	@	proactiveAttachmentStorage.ts:167
await in (anonymous)		
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:636
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:657
<button>		
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:622
(anonymous)	@	ProactiveInitiativeEditorPage.tsx:335
<ProactiveInitiativeEditorPage>		
App	@	App.tsx:115
<App>		
(anonymous)	@	main.tsx:8

download is still not working. please build a simple download function without any restrictions but works first

remove the wording: You can add external links or upload a file. Uploads are stored under the folder from VITE_FIREBASE_STORAGE_ATTACHMENTS_PREFIX in your bucket. File links are not displayed for security — use Download.

add the same attachment function to "Daily Journal" and also replace the attachment link function in "Meeting / Engagement / Activity Reports" with the attachment link function of "Proactive Initiative and Activity"

Title "Edit activity report" should change to "Meeting / Engagement / Activity Reports"

For the "Meeting / Engagement / Activity Reports", change to allowing "save" at anytime

for the "split name" function, other than blindly add names into current ones, also remove those empty name box



### 2026-04-29 ###

For the "Meeting / Engagement / Activity Reports", after split the names of "donor/prospect/guest", add lookup button next to each name. The lookup button will try to match the Name with information in table "V_QUERY_CONSTITUENT". If a match is found, that it will store the lookupid as "constituent id" related to this "donor/prospect/guest". If there are multiple matches, then the system will list out for user's selection. If there is no match, then it should prompt for no match and allow user to enter "constituent id" manually or leave it blank.

There is already a table called "V_QUERY_CONSTITUENT", the schema is as follow:
CREATE TABLE public.v_query_constituent (
	"ID" varchar(40) NULL,
	"KEYNAME" varchar(100) NULL,
	"FIRSTNAME" varchar(50) NULL,
	"MIDDLENAME" varchar(50) NULL,
	"MAIDENNAME" varchar(100) NULL,
	"NICKNAME" varchar(50) NULL,
	"MARITALSTATUSCODEID_TRANSLATION" varchar(100) NULL,
	"LOOKUPID" varchar(100) NULL,
	"TITLECODEID_TRANSLATION" varchar(100) NULL,
	"GENDER" varchar(7) NULL,
	"BIRTHDATE" varchar(8) NULL,
	"DECEASEDDATE" varchar(8) NULL,
	"ISINACTIVE" bit(1) NOT NULL,
	"GIVESANONYMOUSLY" bit(1) NOT NULL,
	"DECEASED" bit(1) NULL,
	"AGE" int4 NULL,
	"DECEASEDYEARS" int4 NULL,
	"NAME" varchar(154) NULL,
	"WEBADDRESS" varchar(2047) NULL,
	"DONOTMAIL" bit(1) NOT NULL,
	"DONOTEMAIL" bit(1) NOT NULL,
	"DONOTPHONE" bit(1) NOT NULL,
	"DATEADDED" timestamp NOT NULL,
	"DATECHANGED" timestamp NOT NULL,
	"PRIMARYBUSINESS_ID" varchar(40) NULL,
	"CONSTITUENTTYPE" varchar(100) NULL,
	"DECEASEDCONFIRMATION" varchar(17) NULL,
	"DECEASEDSOURCECODEID_TRANSLATION" varchar(100) NULL,
	"TITLE2CODEID_TRANSLATION" varchar(100) NULL,
	"FORMATTEDNAME" varchar(404) NULL,
	"ISGROUP" bit(1) NOT NULL,
	"SEQUENCEID" int4 NOT NULL
);

When there are matched constituents for user to select with, offer a view details option for user to view each constituent's detail information so the user will have more information knowing which one is the correct constituent.

The name field user types in may include "title", such as mr miss dr prof..., please incorporate this into search function

lookup function return error in web-browser: @supabase_supabase-js.js?v=85d3f545:19887  POST https://hiesxeucrhohunpqpalk.supabase.co/rest/v1/rpc/search_v_query_constituent 400 (Bad Request)

invalid regular expression: quantifier operand invalid If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

I still got the error: invalid regular expression: quantifier operand invalid If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

The "Select a constituent" pop up should be larger. Better match the browser size.

Even there is only one match, also show the "Select a constituent" and default show details for verification.



### 2026-04-30 ###

If there is multiple matches, unlike single match, the "Select a constituent" should default not show details. For "detail" view in "Select a constituent", remove ID, Title2, Is Group, Inactive, Gives anonymously, date added, date changed, sequence id.

Rebuild the lookup function. Try to see if it contain the any title in the string. If there is a title, then put it aside for later matching. For the rest of the name string, then try to do a name matching. Try to construct a matching score in percentage. And put the matching results in descending order in "select a constituent" pop up.

Failed to run sql query: ERROR:  42601: syntax error at or near ")"
LINE 228:       ),

Constituent lookup is not set up, or the API cache is outdated. In Supabase SQL, run: NOTIFY pgrst, 'reload schema'; Then ensure `search_v_query_constituent` is deployed and `GRANT EXECUTE` applies to the anon role.

i retried but still got the error: Constituent lookup is not set up, or the API cache is outdated. In Supabase SQL, run: NOTIFY pgrst, 'reload schema'; Then ensure `search_v_query_constituent` is deployed and `GRANT EXECUTE` applies to the anon role.

new error message when lookup: Constituent lookup failed (code 42883 — function round(double precision, integer) does not exist — No function matches the given name and argument types. You might need to add explicit type casts.). Confirm VITE_PROFILES_SUPABASE_URL matches the project where you ran the SQL. Try NOTIFY pgrst, 'reload schema'; ensure search_v_query_constituent exists and GRANT EXECUTE applies to anon.

Another error after applying fix: code 57014 — canceling statement due to statement timeout If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

should i create any index for table v_query_constituent?

suggestion: "FORMATTEDNAME", "NAME", "KEYNAME"
"FIRSTNAME", "MAIDENNAME", "NICKNAME"
"TITLECODEID_TRANSLATION", "TITLE2CODEID_TRANSLATION"
"LOOKUPID" (often useful for id/substring search)

another error: code 42702 — column reference "lookupid" is ambiguous — It could refer to either a PL/pgSQL variable or a table column. If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

code 57014 — canceling statement due to statement timeout If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

error when i try to create index: Error: Failed to run sql query: ERROR: 42704: operator class "extensions.gin_trgm_ops" does not exist for access method "gin"

still got error after creating indexes: code 57014 — canceling statement due to statement timeout If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

Change the "Split Name" function, after split names by each lines, also try to match if there is any "title" inside the name. If there is, store the title into another field. Then rewrite the lookup function by matching the names first, and then try to match the title after it.

Put the "Title" box before the "Name" box within the same line. And suspend the matching "title" in lookup function first to trace out if it triggers the timeout issue.

add back the title matching after name matching

Please limit the lookup founction to match only v_query_constituent.nickname and v_query_constituent.name to improve the performance.

You should shuffle the words, split by space, try to match with different combinatiions.

Also try to match v_query_constituent.keyname and v_query_constituent.firstname

split inputted name by space, shuffle them, and then try match only v_query_constituent.keyname, v_query_constituent.firstname and v_query_constituent.nickname

error: Constituent lookup failed (code 42883 — function public._search_tokens(text) does not exist — No function matches the given name and argument types. You might need to add explicit type casts.). Confirm VITE_PROFILES_SUPABASE_URL matches the project where you ran the SQL. Try NOTIFY pgrst, 'reload schema'; ensure search_v_query_constituent exists and GRANT EXECUTE applies to anon.

Constituent lookup failed (code 42883 — function public._perm_phrases_distinct(text[]) does not exist — No function matches the given name and argument types. You might need to add explicit type casts.). Confirm VITE_PROFILES_SUPABASE_URL matches the project where you ran the SQL. Try NOTIFY pgrst, 'reload schema'; ensure search_v_query_constituent exists and GRANT EXECUTE applies to anon.

after clicking "lookup" please show the shuffle result on the screen instead of lookup for debugging.

are you using only permutation phrase for matching 3 fields (v_query_constituent.keyname, v_query_constituent.firstname and v_query_constituent.nickname)? If yes, giving me a timeout is not logical.

change to match the shuffle results with only v_query_constituent.name or v_query_constituent.nickname

please use this logo for the webapp.

stop the debugging of showing shuffle results and do actual lookup.

Put a suggestion to user to type First Name then Last Name, try ty split then names into first name and last name fields, showing at the same line. Then change the lookup function to matching separately Last Name to v_query_constituent.keyname and First Name to v_query_constituent.firstname or v_query_constituent.middlename OR the whole name match v_query_constituent.nickname

Assume only the last word as last name

At the view detail box, please also show "Constituency" from the v_query_constituency.constituency where v_query_constituent.id = v_query_constituency.constituentid. Multiple Consituency should be shown in same line separated by comma.

There should be field constituency in table v_query_constituency. Why it returns error: code 42703 — column vc.constituency does not exist If you recently changed the function, run in Supabase SQL: NOTIFY pgrst, 'reload schema';

remove showing constituency logic


### 2026-05-04 ###

Please reverse the "split name" function back to split into multipe lines but single box, instead of further split title, last name and first name. For the "lookup" function, please use the function search_constituents() in file 001_search_constituents_function.sql. If the user want to see details, then call the function get_constituent_detail() in file 002_get_constituent_detail.sql.

After click on lookup, please show both their lookupid, formattedname, nickname and percentage.

I mean show all in the same line, "formattedname" should just be the name showed before, nickname is chinese name

function search_constituents() of 001_search_constituents_function.sql should return the following:
    y."LOOKUPID",
    y."NICKNAME",
    y."FORMATTEDNAME",
    y."SCORE"

You misintepreted my point, I mean show all the following in same line after clicking the lookup:
    y."LOOKUPID",
    y."NICKNAME",
    y."FORMATTEDNAME",
    y."SCORE"
But currently you are only showing lookupid and score.	
The "View details" should NOT be changed, please revert back using function get_constituent_detail() in file 002_get_constituent_detail.sql.

Change the wording of button "Use this ID" to "Use this Constituent". After clicking on this button, also replace the text in Name box with "formattedname" on top of replacing constituent ID.

Please line up the "lookup" button with name box and "remove" button

Size of lookup button should match with remove button	

At the pop up box, is shows up text: "Several records matched 'XXX'. Each row shows LOOKUPID, NICKNAME, FORMATTEDNAME, and SCORE from the search. Use View details for full CRM fields from get_constituent_detail."
Please repharse to only showing: "Several records matched 'XXX'. Please select the correct one." 

i just want to change the text only, what have you done to the program logic? it does not showing any searching result anymore. please rollback any logic. I just want to change the text.

